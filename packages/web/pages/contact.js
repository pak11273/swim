import { Grid, Layout } from "components"

const Contact = (props) => (
  <Layout title="DP Pools" {...props}>
    <Grid>
      <div>
        <h1>Contact Page</h1>
      </div>
    </Grid>
  </Layout>
)
export default Contact
